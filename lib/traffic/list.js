import { supabaseRequest } from "../_lib/supabase.js";
import { requireRole } from "../_lib/requireRole.js";
export default async function handler(req,res){if(req.method!=="GET"){res.setHeader("Allow","GET");return res.status(405).json({ok:false,error:"Method not allowed"});}const auth=await requireRole(req,["none","member","vice","boss","owner"]);if(!auth.ok)return res.status(auth.status).json({ok:false,error:auth.error});try{const a=String(req.query?.area||"").trim().toLowerCase();const area=["armi","droga"].includes(a)?a:null;const limit=Math.max(1,Math.min(200,Number(req.query?.limit||100)));const rows=await supabaseRequest(`/rest/v1/traffic_records?${area?`area=eq.${area}&`:""}select=*&order=at.desc&limit=${limit}`);const items=(rows||[]).map(x=>({...x,by:x.by_email,byRole:x.by_role}));return res.status(200).json({ok:true,area:area||"all",count:items.length,items});}catch(err){return res.status(500).json({ok:false,error:err?.message||String(err)});}}
